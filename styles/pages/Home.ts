import { ViewS, TextS, rem } from "../../types";

export default {
  container: {
    // paddingTop: 2 * rem,
  },
  text: {
    color: "#fff",
    fontSize: 3 * rem,
  },
} as {
  container: ViewS;
  text: TextS;
};